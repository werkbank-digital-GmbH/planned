/**
 * Globale Konstanten für planned.
 */

// App
export const APP_NAME = 'planned.';

// Arbeitstage
export const WORK_DAYS_PER_WEEK = 5;
export const DEFAULT_WEEKLY_HOURS = 40;

// Pagination
export const DEFAULT_PAGE_SIZE = 20;
export const MAX_PAGE_SIZE = 100;

// Session
export const SESSION_TIMEOUT_DAYS = 7;

// Rate Limiting
export const MAX_LOGIN_ATTEMPTS = 5;
export const LOGIN_LOCKOUT_MINUTES = 15;

// Sync
export const SYNC_INTERVAL_MINUTES = 15;
export const ABSENCES_SYNC_DAYS_AHEAD = 30;
export const TIME_ENTRIES_SYNC_HOURS_BACK = 2;

// Undo/Redo
export const MAX_UNDO_STACK_SIZE = 50;

// Validation
export const MIN_PASSWORD_LENGTH = 8;
export const MAX_NOTE_LENGTH = 500;
export const MAX_PROJECT_NAME_LENGTH = 100;
export const MAX_PHASE_NAME_LENGTH = 100;
